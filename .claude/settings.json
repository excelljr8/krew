{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/alvarojr/krew && git add -A && git diff --cached --quiet || (git commit -m 'auto: sync changes to GitHub' && git push origin main)",
            "statusMessage": "Pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
